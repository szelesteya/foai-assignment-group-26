[project]
name = "foai-assignment-group-26"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.5",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
